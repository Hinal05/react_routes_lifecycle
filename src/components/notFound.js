import React from 'react'; // ESX
import { Link } from 'react-router-dom';

const notFound = () => {
  return (
    <div>
      404
    </div>
  )
}

export default notFound;  // If you need to use this component for other component you need to available and export for them.
