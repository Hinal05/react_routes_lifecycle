import React from 'react'; // ESX

const Home = () => {
  return (
    <div>
      Home
    </div>
  )
}

export default Home;  // If you need to use this component for other component you need to available and export for them.
